# Rendering imports bartorch from src/, which needs torch (a CPU build is
# enough) but not the compiled library.
numpy>=1.24
torch>=2.2
# The denoiser the deep-learning example unrolls, and what it is trained
# with.  Only `--execute` needs them; they are here rather than in a list of
# their own because that is where the gallery's other dependencies are.
deepinv>=0.2
lightning>=2.2
torchio>=0.19
monai>=1.3
sphinx>=7.3,<9
sphinx-book-theme>=1.1,<2
sphinx-copybutton>=0.5,<1
sphinx-gallery>=0.16,<1
myst-parser>=3,<5
# The gallery's figures.  Rendering the example pages needs it as well as
# running them, because the scraper is loaded either way.
matplotlib>=3.7
# The perceptually uniform colormaps the relaxometry examples are drawn with:
# lipari for T1 and navia for T2, as Fuderer et al. recommend.
cmap>=0.6
