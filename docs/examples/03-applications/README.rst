Applications
------------

Two acquisitions whose encoding carries more than sampling and coils.

A golden-angle radial series is reconstructed frame by frame with a temporal
regularizer, and an inversion-recovery experiment is reconstructed into the
coefficients of a signal subspace, from which :math:`T_1` is estimated.
